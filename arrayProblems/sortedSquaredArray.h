#ifndef INTERVIEW_SORTEDSQUAREDARRAY_H
#define INTERVIEW_SORTEDSQUAREDARRAY_H

#include <vector>
#include <algorithm>
#include <cmath>

std::vector<int> sortedSquaredArrayNaiveSolution(std::vector<int> arr);
std::vector<int> sortedSquaredArrayOptimized(std::vector<int> arr);

#endif //INTERVIEW_SORTEDSQUAREDARRAY_H
